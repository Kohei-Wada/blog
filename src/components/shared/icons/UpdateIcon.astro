---
/**
 * Update Icon (wrapper for backward compatibility)
 */
import Icon from './Icon.astro';

interface Props {
  width?: number;
  height?: number;
  class?: string;
}

const { width = 16, height = 16, class: className = '' } = Astro.props;
---

<Icon name="update" width={width} height={height} class={className} />
