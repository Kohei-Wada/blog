---
import BaseLayout from './BaseLayout.astro';
import FormattedDate from '../components/shared/ui/FormattedDate.astro';
import CalendarIcon from '../components/shared/icons/CalendarIcon.astro';
import UpdateIcon from '../components/shared/icons/UpdateIcon.astro';
import ShareButtons from '../components/integrations/ShareButtons.astro';
import TableOfContents from '../components/blog/navigation/TableOfContents.astro';
import RelatedPosts from '../components/blog/content/RelatedPosts.astro';
import type { CollectionEntry } from 'astro:content';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags?: string[];
  headings?: import('../types/index.js').MarkdownHeading[];
  relatedPosts?: CollectionEntry<'blog'>[];
}

const {
  title,
  description,
  pubDate,
  updatedDate,
  tags = [],
  headings = [],
  relatedPosts = [],
} = Astro.props;

// 現在のページのURLを取得
const currentUrl = new URL(Astro.url.pathname, Astro.site).toString();
---

<BaseLayout
  title={title}
  description={description}
  isArticle={true}
  publishedTime={pubDate}
  showSidebar={true}
>
  <style>
    article {
      width: 100%;
    }
    .prose {
      width: 100%;
      margin: 0;
      padding: 0;
      color: var(--color-text);
    }
    .title {
      margin-bottom: 1em;
      padding: 1em 0;
      text-align: center;
      line-height: 1;
    }
    .title h1 {
      margin: 0 0 0.5em 0;
    }
    .date {
      margin-bottom: 0.5em;
      color: var(--color-text-secondary);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      flex-wrap: wrap;
    }
    .last-updated-on,
    .published-on {
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }
  </style>

  <div class="page-container">
    <article>
      <div class="prose">
        <div class="title">
          <div class="date">
            {
              pubDate && (
                <div class="published-on">
                  <CalendarIcon />
                  <FormattedDate date={pubDate} />
                </div>
              )
            }

            {
              updatedDate && (
                <div class="last-updated-on">
                  <UpdateIcon />
                  <FormattedDate date={updatedDate} />
                </div>
              )
            }
          </div>
          <h1>{title}</h1>
          <hr />
        </div>

        {
          tags.length > 0 && (
            <div class="tag-list center">
              {tags.map((tag: string) => (
                <a href={`/tags/${tag}/`} class="tag blog-post">
                  {tag}
                </a>
              ))}
            </div>
          )
        }

        <TableOfContents headings={headings} />

        <slot />
        <ShareButtons title={title} description={description} url={currentUrl} />
        <RelatedPosts posts={relatedPosts} currentTags={tags} />
      </div>
    </article>
  </div>
</BaseLayout>
